const getAllElement = require('./getAllElement.middleware');
const getElement = require('./getElement.middleware');
const replaceElement = require('./replaceElement.middleware');

module.exports = {
    getAllElement,
    getElement,
    replaceElement
}