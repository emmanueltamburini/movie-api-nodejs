const getAllElement = require('./getAllElement.midleware');
const getElement = require('./getElement.midleware');
const replaceElement = require('./replaceElement.midleware');

module.exports = {
    getAllElement,
    getElement,
    replaceElement
}